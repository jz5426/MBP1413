source: "huggingface" # one of { "huggingface"}
name: emilyalsentzer/Bio_ClinicalBERT
pretrained: ${designs.model.text_encoder.pretrained}
gradient_checkpointing: false
pooling: ${designs.model.text_encoder.pooling}  # one of { "eos" | "bos" | "mean" }
cache_dir: '/cluster/projects/mcintoshgroup/CXR-CLIP/models--emilyalsentzer--Bio_ClinicalBERT/'
trust_remote_code: true
mlm_head: true # TODO: what is this.
dual_cls: ${designs.tokenizer.text_dual_cls}